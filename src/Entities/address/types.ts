export interface Address {
  id: string,
  name: string;
  cep: string;
  address_type: string,
  address_name: string,
  state: string,
  district: string,
  city: string,
  lat: number,
  lng: number,
}
